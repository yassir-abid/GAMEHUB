<!DOCTYPE html>
<html lang="fr">

<head>
    <meta charset="UTF-8">
    <title>GameHub</title>
    <link rel="stylesheet" href="/css/reset.css">
    <link rel="stylesheet" href="/css/style.css">
    <% if (locals.error) { %>
        <link rel="stylesheet" href="/css/error.css">
        <% } %>
    <% if (locals.searchedGame) { %>
        <link rel="stylesheet" href="/css/<%= locals.searchedGame.cssFile %>">
        <% } %>
</head>

<body>
    <header>
        <h1>
            <a href="/">GameHub</a>
        </h1>

        <nav>
            <ul>
                <% for (const game of games) { %>
                    <li><a href="/game/<%= game.name %>">
                            <%= game.title %>
                        </a></li>
                    <% } %>
            </ul>
        </nav>

    </header>